// ===========================================================================
//  Book configuration.
//  One place for the things that describe *this* book rather than the reader.
//  `lang` drives the reading column's lang attribute, which is what the browser
//  uses to pick a hyphenation dictionary for justified text — set it to 'nl'
//  when the Dutch text lands and hyphenation follows automatically.
// ===========================================================================

export const BOOK = {
  title: 'The Book',
  lang: 'en',
}
